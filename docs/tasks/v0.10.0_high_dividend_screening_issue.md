---
category: tasks
milestone: v0.10.0
ai_context: high
last_updated: 2026-02-17
related_docs:
  - ./milestones.md
  - ../high_dividend_screening_design.md
  - ../develop-guide/development-workflow.md
---

# v0.10.0 - 高配当株スクリーニング 実装Issue一覧

## 概要
高配当株スクリーニング設計書（../high_dividend_screening_design.md）に基づき、実装を小さなIssueに分割した一覧です。各Issueは実装対象ファイル、テスト、完了条件を含みます。

**マイルストーン期間:** 2週間程度（フェーズ毎に調整）
**優先度:** 🟢 High

---

## 対応予定 Issue（概要）

1. [FEATURE] `FinancialAnalysisService` 実装
2. [FEATURE] `ScreeningResult` / `WatchList` モデルとマイグレーション
3. [FEATURE] Repository 実装（ScreeningResult / WatchList / Rule）
4. [FEATURE] `RuleEngine` 実装（ルール定義フォーマットとプラグイン演算子）
5. [FEATURE] `ScreeningService` 実装（同期バッチ・API連携）
6. [FEATURE] `WatchListService` 実装（株価更新・購入候補抽出）
7. [FEATURE] API エンドポイント追加（/api/v1/screening, /api/v1/watch-list）
8. [FEATURE] バッチスクリプト追加（`batch_screening.py`, `batch_update_watch_list_prices.py`）
9. [TEST] 単体・統合テストの追加とCI設定
10. [DOC] ルールセット（YAML）整備と運用手順

---

## 設計書→ソースファイルマッピング（Issue単位）

- Issue 1: `FinancialAnalysisService` 実装
  - `app/services/screening/financial_analysis_service.py`
  - `app/services/screening/__init__.py` (エクスポート)
  - `tests/unit/services/screening/test_financial_analysis_service.py`

- Issue 2: `ScreeningResult` / `WatchList` モデルとマイグレーション
  - `app/models/screening/screening_result.py`
  - `app/models/screening/watch_list.py`
  - `app/models/screening/__init__.py` (エクスポート)
  - `alembic/versions/<timestamp>_create_screening_and_watchlist_tables.py`
  - `tests/unit/models/test_screening_models.py`

- Issue 3: Repository 実装
  - `app/repositories/screening/screening_result_repository.py`
  - `app/repositories/screening/watch_list_repository.py`
  - `app/repositories/rule_repository.py`
  - `tests/unit/repositories/test_screening_repositories.py`

- Issue 4: `RuleEngine` 実装
  - `app/services/screening/rule_engine.py`
  - `app/rules/core_v1.yaml` (例)
  - `tests/unit/services/screening/test_rule_engine.py`

- Issue 5: `ScreeningService` 実装
  - `app/services/screening/screening_service.py`
  - `scripts/batch/batch_screening.py`
  - `tests/integration/services/test_screening_service_integration.py`

- Issue 6: `WatchListService` 実装
  - `app/services/screening/watch_list_service.py`
  - `scripts/batch/batch_update_watch_list_prices.py`
  - `tests/unit/services/screening/test_watch_list_service.py`

- Issue 7: API エンドポイント
  - `app/api/v1/screening.py` (POST /api/v1/screening/execute, GET /api/v1/screening/results)
  - `app/api/v1/watch_list.py` (POST /api/v1/watch-list/update-prices 等)
  - `tests/unit/api/test_screening_api.py`

- Issue 8: バッチと運用スクリプト
  - `scripts/batch/batch_screening.py`
  - `scripts/batch/batch_update_watch_list_prices.py`
  - `docs/deploy/screening_runbook.md` (運用手順)

- Issue 9: テスト/CI
  - `tests/unit/...`, `tests/integration/...` の追加
  - CI ワークフロー: `.github/workflows/ci.yml` のテストジョブ更新

- Issue 10: ドキュメントとルール運用
  - `app/rules/` にルールセット配置
  - `docs/` にルール運用手順を追記

---

## Issueテンプレート例（Issue 1 のサンプル）

---
name: 新機能実装
about: 財務分析サービスを実装する
title: "[FEATURE] FinancialAnalysisService 実装"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: service, priority: high'
assignees: ''
milestone: v0.10.0
---

## 概要
過去 5 年分の配当・EPS・営業CF 等を取得し、トレンド計算（Kendallのτ 等）や指標計算を行う `FinancialAnalysisService` を実装します。

## 実装ファイル
- `app/services/screening/financial_analysis_service.py`
- `app/services/screening/__init__.py` (エクスポート)
- `tests/unit/services/screening/test_financial_analysis_service.py`

## 実装内容（要点）
- `get_dividend_history(sec_code, years=5)` を実装
- `calculate_dividend_trend(dividends)`（Kendallのτ）を実装
- `get_eps_history`, `calculate_eps_trend` を実装
- `get_operating_cf_history`, `calculate_operating_margin_trend` を実装

## テスト
- 単体テストを作成し、外部API呼び出しはモック化する

## 完了条件
- メソッド群が実装され、型ヒントとdocstringが存在する
- 単体テストが追加され、CIで成功する

---

## 補足・実装優先度
- 優先度は次の通り: Issue1 → Issue2 → Issue3 → Issue4 → Issue5 → Issue6 → Issue7 → Issue8 → Issue9 → Issue10
- まずは `FinancialAnalysisService` とモデル（`ScreeningResult`）を実装して、年次バッチが動く状態を作ることを推奨します。

---

※ このファイルを基に、個々のIssueを GitHub に作成する場合は、上記の「Issueテンプレート例」を Issue の本文に貼り付けて利用してください。

---

## 各Issueのテンプレート（すべて）

以下は Issue1〜Issue10 のテンプレートです。コピーして GitHub Issue の本文としてお使いください。

---

<!-- Issue 1 -->
name: 新機能実装
about: 財務分析サービスを実装する
title: "[FEATURE] FinancialAnalysisService 実装"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: service, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
過去 5 年分の配当・EPS・営業CF 等を取得し、トレンド計算（Kendallのτ 等）や指標計算を行う `FinancialAnalysisService` を実装します。

## 実装ファイル
- `app/services/screening/financial_analysis_service.py`
- `app/services/screening/__init__.py` (エクスポート)
- `tests/unit/services/screening/test_financial_analysis_service.py`

## 実装内容（要点）
- `get_dividend_history(sec_code, years=5)` を実装
- `calculate_dividend_trend(dividends)`（Kendallのτ）を実装
- `get_eps_history`, `calculate_eps_trend` を実装
- `get_operating_cf_history`, `calculate_operating_margin_trend` を実装

## テスト
- 単体テストを作成し、外部API呼び出しはモック化する

## 完了条件
- メソッド群が実装され、型ヒントとdocstringが存在する
- 単体テストが追加され、CIで成功する

---

<!-- Issue 2 -->
name: 新機能実装
about: ScreeningResult と WatchList のモデルとマイグレーションを追加する
title: "[FEATURE] ScreeningResult / WatchList モデル追加とマイグレーション"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: models, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
スクリーニング結果と監視リストを永続化するための ORM モデルと Alembic マイグレーションを実装します。

## 実装ファイル
- `app/models/screening/screening_result.py`
- `app/models/screening/watch_list.py`
- `app/models/screening/__init__.py` (エクスポート)
- `alembic/versions/<timestamp>_create_screening_and_watchlist_tables.py`
- `tests/unit/models/test_screening_models.py`

## 実装内容（要点）
- `ScreeningResult` のカラム定義（specに合わせる）
- `WatchList` のカラム定義
- マイグレーションファイルを生成・調整

## テスト
- モデル生成テスト、ユニーク制約やインデックスのテスト

## 完了条件
- モデルが追加され、マイグレーションでテーブル作成が可能であること
- 単体テストが追加され、CIで成功する

---

<!-- Issue 3 -->
name: 新機能実装
about: ScreeningResult / WatchList / Rule 用の Repository を実装する
title: "[FEATURE] ScreeningResult/WatchList/Rule Repository 実装"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: repository, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
モデルに対する CRUD、UPSERT、検索機能を提供する Repository を実装します。

## 実装ファイル
- `app/repositories/screening/screening_result_repository.py`
- `app/repositories/screening/watch_list_repository.py`
- `app/repositories/rule_repository.py`
- `tests/unit/repositories/test_screening_repositories.py`

## 実装内容（要点）
- `upsert` の実装（submission_date 等の条件付き更新）
- ページネーション・一括取得メソッド

## テスト
- CRUD/UPSERT のユニットテスト

## 完了条件
- Repository の主要メソッドが実装され、テストが通ること

---

<!-- Issue 4 -->
name: 新機能実装
about: ルールエンジンとルール定義フォーマットを実装する
title: "[FEATURE] RuleEngine 実装（ルール定義 + プラグイン演算子）"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: service, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
YAML/JSONで定義されたルールを読み込み、プラグイン方式で条件演算子を評価する `RuleEngine` を実装します。

## 実装ファイル
- `app/services/screening/rule_engine.py`
- `app/rules/core_v1.yaml` (例)
- `tests/unit/services/screening/test_rule_engine.py`

## 実装内容（要点）
- ルール読み込み・バリデーション
- 条件（operator）プラグイン登録と評価
- ルールセットのバージョン・有効フラグ対応

## テスト
- 代表的な条件の評価テスト、バリデーションテスト

## 完了条件
- `RuleEngine` がルールを正しく評価できること

---

<!-- Issue 5 -->
name: 新機能実装
about: ScreeningService を実装し、年次バッチを作成する
title: "[FEATURE] ScreeningService 実装 + 年次バッチ"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: service, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
`RuleEngine` と `FinancialAnalysisService` を組み合わせ、銘柄一覧のスクリーニング実行→`ScreeningResult` 永続化を行う `ScreeningService` と同期バッチを実装します。

## 実装ファイル
- `app/services/screening/screening_service.py`
- `scripts/batch/batch_screening.py`
- `tests/integration/services/test_screening_service_integration.py`

## 実装内容（要点）
- 全銘柄(or 指定銘柄)の同期実行フロー
- ルールセット切替、スコア計算、永続化

## テスト
- 統合テスト（モック依存を最小化）

## 完了条件
- 年次バッチでスクリーニング→DB登録が可能であること

---

<!-- Issue 6 -->
name: 新機能実装
about: WatchListService と日次株価更新バッチを実装する
title: "[FEATURE] WatchListService 実装 + 日次株価更新バッチ"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: service, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
監視リストの作成・更新、株価取得による配当利回りの再計算、購入候補抽出を行う `WatchListService` と日次バッチを実装します。

## 実装ファイル
- `app/services/screening/watch_list_service.py`
- `scripts/batch/batch_update_watch_list_prices.py`
- `tests/unit/services/screening/test_watch_list_service.py`

## 実装内容（要点）
- 監視閾値（70点）での追加/削除
- 株価更新と配当利回り計算（外部APIはモック化）

## テスト
- 単体テストで株価更新ロジック・購入候補抽出を検証

## 完了条件
- 日次バッチが監視リストを更新し、購入候補を抽出できること

---

<!-- Issue 7 -->
name: 新機能実装
about: スクリーニング・監視リスト用の API を追加する
title: "[FEATURE] API: /api/v1/screening, /api/v1/watch-list 追加"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: api, priority: medium'
assignees: ''
milestone: v0.10.0

## 概要
スクリーニング実行や結果取得、監視リストの管理用 API エンドポイントを追加します。

## 実装ファイル
- `app/api/v1/screening.py`
- `app/api/v1/watch_list.py`
- `tests/unit/api/test_screening_api.py`

## 実装内容（要点）
- `POST /api/v1/screening/execute` 実行エンドポイント
- `GET /api/v1/screening/results` 取得エンドポイント
- `POST /api/v1/watch-list/update-prices` 等

## テスト
- API レスポンスのユニットテスト

## 完了条件
- エンドポイントが実装され、ルーティング・認可（必要）で動作すること

---

<!-- Issue 8 -->
name: 新機能実装
about: バッチスクリプトと運用ドキュメントを追加する
title: "[FEATURE] バッチスクリプト追加 & 運用Runbook"
labels: 'type: feature, status: todo, ms: v0.10.0, layer: infra, priority: medium'
assignees: ''
milestone: v0.10.0

## 概要
運用のためのバッチスクリプトと運用手順ドキュメントを追加します。

## 実装ファイル
- `scripts/batch/batch_screening.py`
- `scripts/batch/batch_update_watch_list_prices.py`
- `docs/deploy/screening_runbook.md`

## 実装内容（要点）
- 実行用 CLI/エントリ、環境変数やスケジューリング手順を記載

## テスト
- ローカル手順の検証ドキュメントレビュー

## 完了条件
- Runbook に沿ってバッチが実行できること

---

<!-- Issue 9 -->
name: テスト整備
about: 単体・統合テストの追加と CI 更新
title: "[TEST] カバレッジ増強と CI ワークフロー更新"
labels: 'type: test, status: todo, ms: v0.10.0, priority: high'
assignees: ''
milestone: v0.10.0

## 概要
追加した機能群のユニット・統合テストを整備し、CI ワークフローを更新します。

## 実装ファイル
- `tests/unit/...`, `tests/integration/...` の追加
- `.github/workflows/ci.yml` のテストジョブ更新

## 実装内容（要点）
- モックを使った単体テスト、軽量な統合テストの作成
- CI のテスト実行（pytest, coverage）を組み込む

## 完了条件
- CI がテストを実行して成功すること

---

<!-- Issue 10 -->
name: ドキュメント
about: ルールセット整備と運用手順のドキュメント化
title: "[DOC] ルールセット整備 & 運用手順"
labels: 'type: docs, status: todo, ms: v0.10.0, priority: low'
assignees: ''
milestone: v0.10.0

## 概要
運用ルールセット（YAML）と運用手順・監視手順を整備します。

## 実装ファイル
- `app/rules/core_v1.yaml`
- `docs/deploy/screening_runbook.md`

## 実装内容（要点）
- 代表的なルールセットの作成
- ルール追加/バージョン管理手順の記載

## 完了条件
- ルールセットが運用可能で、運用手順が文書化されていること
