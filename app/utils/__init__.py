"""`app.utils` パッケージのユーティリティ初期化モジュール.

このモジュールは副作用を最小化するよう設計されています。具体的な
ユーティリティ（例：`app.utils.logger` や `app.utils.database`）は
必要に応じて個別にインポートしてください。パッケージインポート時に
初期化処理を行うと起動時の挙動が不安定になるため避けてください。

ノート:
        パッケージレベルでのロガー初期化やデータベースヘルパーのインポートは
        行わないでください。アプリケーションの起動順序を予測可能に保つためです。
"""

__all__: list[str] = []
